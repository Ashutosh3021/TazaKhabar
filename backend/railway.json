{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "DOCKERFILE"
    },
    "deploy": {
        "healthcheckPath": "/health",
        "startCommand": "sh -c 'hypercorn src.main:app --bind 0.0.0.0:${PORT:-8000}'",
        "numReplicas": 1
    }
}
